
## TQC Java 17 物件導向程式設計練習題批改結果

**總分：79 / 100**

---

### 第一部分：選擇題 (共 20 題，每題 2 分，共 40 分)

**您的得分：26/40**

**詳細批改：**

1.  **題目：** 下列何者是 Java 中所有類別的父類別？
    **您的答案：** b
    **正確答案：** b (`Object`)
    **結果：** 正確

2.  **題目：** 關於抽象類別 (abstract class) 和介面 (interface) 的敘述，下列何者**錯誤**？
    **您的答案：** abcd
    **正確答案：** c (一個類別可以繼承多個抽象類別是錯誤的，Java 不支援多重繼承抽象類別，但可以實作多個介面。)
    **結果：** 錯誤

3.  **題目：** 在 Java 中，`final` 關鍵字可以用於何處？
    **您的答案：** d
    **正確答案：** a (類別、方法、變數)
    **結果：** 錯誤

4.  **題目：** 下列哪一個關鍵字用於處理例外情況？
    **您的答案：** d
    **正確答案：** d (`try`, `catch`, `finally` 和 `throw`, `throws` 都用於處理例外情況)
    **結果：** 正確

5.  **題目：** 關於 `static` 關鍵字的敘述，何者正確？
    **您的答案：** a
    **正確答案：** c (`static` 變數又稱為類別變數)
    **結果：** 錯誤

6.  **題目：** 下列何者是正確的 `main` 方法簽章？
    **您的答案：** b
    **正確答案：** b (`public static void main(String[] args)`)
    **結果：** 正確

7.  **題目：** 關於方法多載 (Overloading) 和方法覆寫 (Overriding) 的比較，何者**錯誤**？
    **您的答案：** c
    **正確答案：** d (`private` 方法不能被覆寫)
    **結果：** 錯誤

8.  **題目：** 下列哪個集合 (Collection) 不允許儲存重複的元素？
    **您的答案：** b
    **正確答案：** b (`Set`)
    **結果：** 正確

9.  **題目：** `"hello".equals("Hello")` 的回傳值是什麼？
    **您的答案：** b
    **正確答案：** b (`false`，因為 `equals()` 區分大小寫)
    **結果：** 正確

10. **題目：** 若要建立一個 `StringBuilder` 物件，下列何者語法正確？
    **您的答案：** d
    **正確答案：** b (`StringBuilder sb = new StringBuilder("hello");`)
    **結果：** 錯誤

11. **題目：** 關於 Java 17 的 `record` 特性，下列敘述何者正確？
    **您的答案：** b
    **正確答案：** b (`record` 的欄位預設是 `final` 的)
    **結果：** 正確

12. **題目：** `Math.round(2.5)` 的回傳值是什麼？
    **您的答案：** d
    **正確答案：** c (`3`)
    **結果：** 錯誤

13. **題目：** 在子類別的建構子中，若要呼叫父類別的建構子，應該使用哪個關鍵字？
    **您的答案：** b
    **正確答案：** b (`super()`)
    **結果：** 正確

14. **題目：** `instanceof` 運算子的作用是什麼？
    **您的答案：** b
    **正確答案：** b (檢查一個物件是否為某個類別或其子類別的實體)
    **結果：** 正確

15. **題目：** `String` 和 `StringBuilder` 的主要區別是什麼？
    **您的答案：** a
    **正確答案：** a (`String` 是不可變的 (immutable)，`StringBuilder` 是可變的 (mutable))
    **結果：** 正確

16. **題目：** 下列何者不是 Java 的基本資料型態 (primitive data type)？
    **您的答案：** c
    **正確答案：** c (`String`)
    **結果：** 正確

17. **題目：** 執行以下程式碼，會輸出什麼？
    ```java
    List<String> list = new ArrayList<>();
    list.add("A");
    list.add("B");
    list.set(1, "C");
    System.out.println(list);
    ```
    **您的答案：** d
    **正確答案：** b (`[A, C]`)
    **結果：** 錯誤

18. **題目：** 關於封裝 (Encapsulation) 的概念，下列敘述何者最佳？
    **您的答案：** b
    **正確答案：** b (將資料 (屬性) 和操作資料的方法 (行為) 綁定在一起，並對外部隱藏實現細節)
    **結果：** 正確

19. **題目：** 下列哪段程式碼可以成功捕捉 `ArrayIndexOutOfBoundsException`？
    ```java
    try {
        int[] arr = new int[5];
        System.out.println(arr[5]);
    }
    // MISSING CODE
    ```
    **您的答案：** d
    **正確答案：** d (以上皆是，`ArrayIndexOutOfBoundsException` 繼承自 `IndexOutOfBoundsException`，而 `IndexOutOfBoundsException` 繼承自 `Exception`)
    **結果：** 正確

20. **題目：** 在 Java 17 中，`switch` 運算式 (expression) 與傳統 `switch` 陳述式 (statement) 的主要不同之處在於？
    **您的答案：** d
    **正確答案：** d (以上皆是)
    **結果：** 正確

---

### 第二部分：實作題 (共 3 題，共 60 分)

#### **第一題：員工薪資計算 (20 分)**

**您的得分：20/20**

**批改意見：**
*   程式碼結構 (抽象類別 `Employee`、子類別 `FullTimeEmployee` 和 `PartTimeEmployee`) 符合題目要求。
*   各類別的屬性、建構子和方法實作邏輯正確。
*   `Main` 類別能夠正確建立物件、計算並印出薪資。
*   輸出的格式和內容與預期一致。

#### **第二題：圖形面積計算 (20 分)**

**您的得分：13/20**

**批改意見：**
*   **`Shape.java`：**
    *   `PI` 常數的值設定錯誤。題目要求 `3.14159`，您設定為 `3.1459`，這導致圓形面積計算結果不精確。**扣 5 分。**
*   **`Main.java`：**
    *   輸出的格式化要求是小數點後兩位 (`%.2f`)，但您使用了小數點後一位 (`%.1f`)。**扣 2 分。**
*   介面 `Shape` 的定義以及 `Circle` 和 `Rectangle` 類別的實作邏輯是正確的。

#### **第三題：圖書管理系統 (20 分)**

**您的得分：20/20**

**批改意見：**
*   `Book` record 的使用正確，並正確包含了 `isbn`, `title`, `author` 欄位。
*   `Library` 類別使用 `Map` 管理書籍，並正確實作了 `addBook`、`findBookByIsbn` 和 `displayAllBooks` 方法。
*   `Main` 類別正確展示了新增、顯示和尋找書籍的功能。
*   雖然 `Book` record 的 `toString()` 方法和 `findBookByIsbn` 方法的輸出與範例輸出在格式細節上有微小差異 (例如額外的換行符號、搜尋訊息的措辭)，但核心功能和邏輯完全符合要求。

---

**總結：**
您在物件導向的基本概念和實作方面表現良好，特別是第一題和第三題。第二題的主要失分點在於對常數值的精確度和輸出格式的細節處理。請注意這些細節，它們在實際開發中也很重要。
